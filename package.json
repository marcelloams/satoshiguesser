{
  "name": "satoshi-guesser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch:patoshi": "node scripts/fetch-patoshi.js",
    "build:wallets": "node scripts/build-wallet-set.js",
    "prebuild": "npm run build:wallets",
    "predev": "npm run build:wallets",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@noble/hashes": "^1.4.0",
    "@noble/secp256k1": "^2.1.0",
    "@scure/base": "^1.1.6",
    "canvas-confetti": "^1.9.3"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
